node_modules/
.next/
out/
build/
.env
.env.local
*.db
*.db-journal
prisma/*.db
prisma/migrations/dev*
.DS_Store
coverage/

# Local Claude Code preview config
.claude/
tsconfig.tsbuildinfo
*.tsbuildinfo
.superpowers/
.playwright-mcp/

# python bytecode (garmin-sync)
__pycache__/
*.pyc

# scratch / sqlite sidecar artifacts
*.db-shm
*.db-wal
*.db.bak-*
prisma/prisma/
prisma/scratch*.db
.env.designdev

# Private / local-only files — real hosts, LAN IPs, notify services, deploy
# specifics. Keep your filled-in copy as <name>.local.md next to the public
# template. (Covers DEPLOY.local.md — the homelab deploy plan w/ unraid + the
# restricted SSH filter — and docs/*.local.md deploy-ready snippets.)
*.local.md
*.local.yaml
*.local.yml
*.local.json
